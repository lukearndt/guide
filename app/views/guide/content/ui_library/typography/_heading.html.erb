<div class="sg-content">
  <div class="sg-content__intro">
    <p class="t-body -size-l">
      There are no default styles applied to <code class="sg-code">h1-h6</code>
      <br class="is-hidden-tablet-and-below" />
      instead we use utility classes to style all heading on demand for greater control.
    </p>
  </div>

  <section class="sg-content__group">
    <h2 class="t-heading -size-m">Sizes</h2>
    <p class="t-body">Heading sizes can be applied to any heading element but please use your SEO common sense.</p>

    <div class="sg-content__example">
      <%= code_example('markup') do %>
        <!-- Extra Extra Large -->
        <h1 class="t-heading -size-xxl">Heading</h1>

        <!-- Extra Large -->
        <h1 class="t-heading -size-xl">Heading</h1>

        <!-- Large -->
        <h1 class="t-heading -size-l">Heading</h1>

        <!-- Medium -->
        <h1 class="t-heading -size-m">Heading</h1>

        <!-- Small -->
        <h1 class="t-heading -size-s">Heading</h1>

        <!-- Extra Small -->
        <h1 class="t-heading -size-xs">Heading</h1>
      <% end %>
    </div>
  </section>

  <section class="sg-content__group">
    <h2 class="t-heading -size-m">Links</h2>
    <p class="t-body">Headings are able to contain links. They have default styling but also a few other options.</p>

    <div class="alert-box">
      <div class="alert-box__icon">
        <i class="e-icon -icon-flag"></i>
      </div>
      <div class="alert-box__message">
        <p class="t-body -size-m h-remove-margin">
          <strong>Note:</strong> For anything other than default link styling, use <code class="sg-code -color-light">t-link</code> <%= link_to "styles", Guide::Engine.routes.url_helpers.node_path('ui_library/typography/link') %> directly on the link.
        </p>
      </div>
    </div>

    <div class="sg-content__example">
      <%= code_example('markup') do %>
        <!-- Default link style -->
        <h1 class="t-heading -size-s">Heading with <%= link_to "link", "#" %></h1>
      <% end %>
    </div>
  </section>

  <section class="sg-content__group">
    <h2 class="t-heading -size-m">Colors</h2>
    <p class="t-body">Heading colors are purposely limited, however they can inherit color from their parent. Most times you will only want to change the color if the background is dark.</p>

    <div class="sg-content__example">
      <%= code_example('markup') do %>
        <span class="sg-code--block example">
          <!-- Default (Inherits color from base styles) -->
          <h1 class="t-heading -size-m">Heading default color</h1>
        </span>

        <span class="sg-code--block example">
          <!-- Dark text for light backgrounds -->
          <h1 class="t-heading -size-m -color-dark">Heading dark color</h1>
        </span>

        <span class="sg-code--block example">
          <!-- Light text for dark backgrounds -->
          <div style="background:#82b541;">
            <h1 class="t-heading -size-m -color-light">Heading light color</h1>
          </div>
        </span>

        <span class="sg-code--block example">
          <!-- Highlight color -->
          <h1 class="t-heading -size-m -color-highlight">Heading highlight color</h1>
        </span>

        <span class="sg-code--block example">
          <!-- Inherit colour from parent -->
          <span style="color:orange;">
            <h1 class="t-heading -size-m -color-inherit">Heading inherit color</h1>
          </span>
        </span>
      <% end %>
    </div>
  </section>

  <section class="sg-content__group">
    <h2 class="t-heading -size-m">Weights</h2>
    <p class="t-body">There may be a special case were you need to unbolden a heading. Use these sparingly.</p>

    <div class="sg-content__example">
      <%= code_example('markup') do %>
        <!-- Heading default weight (600) -->
        <h1 class="t-heading -size-s">Heading default weight</h1>

        <!-- Heading normal weight -->
        <h1 class="t-heading -size-s -weight-normal">Heading normal weight</h1>

        <!-- Heading light weight -->
        <h1 class="t-heading -size-s -weight-light">Heading light weight</h1>
      <% end %>
    </div>
  </section>
</div>
