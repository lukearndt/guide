<div class="sg-content">
  <div class="sg-content__intro">
    <p class="t-body -size-l">These are Icons. Try it! The Envato icons, these are generated through the `grunt glyphs` task in `marketplace_assets`. You declare the `e-icon` class and then through chainable modifiers can control the appearance of your icon and alter things like size, colour, margin, rotation and flip.</p>
  </div>

  <section class="sg-content__group">
    <h2 class="t-heading -size-m">Icons</h2>
    <p class="t-body">The primary part of the icon is the...icon itself!</p>

    <div class="alert-notification">
      <p class="t-body -size-m h-remove-margin">The Envato Market icon-font and the outputted glyphs below are managed and dynamically generated by <a href="https://github.com/envato/marketplace_assets">Marketplace Assets repo</a></p>
    </div>

    <%= render 'guide/content/ui_library/ui_components/fixtures/glyphs' %>

  </section>


  <section class="sg-content__group">
    <h2 class="t-heading -size-m">Size</h2>
    <p class="t-body">Only sizes in use have been created. If you need another size please add it to the css and add an example here</p>

    <% icon_sizes = [
      "default",
      "-size-medium",
      "-size-large",
      "-size-jumbo",
      "-size-enormous",
    ] %>

    <div class="sg-grid">
      <% icon_sizes.each do |size| %>
        <div class="sg-grid__item">
          <div class="sg-swatch">
            <div class="sg-swatch__header -size-large">
              <i class="e-icon -icon-envato <%= size %>"></i>
            </div>
            <div class="sg-swatch__body">
              <code><%= size %></code>
            </div>
          </div>
        </div>
      <% end %>
    </div>

    <div class="sg-content__example">
      <h3 class="t-heading -size-xs">Size Example</h3>
      <%= code_example do %>
        <span class="sg-code--inline example">
          <!-- A jumbo icon -->
          <i class="e-icon -icon-envato -size-jumbo"></i>
        </span>
      <% end %>
    </div>
  </section>


  <section class="sg-content__group">
    <h2 class="t-heading -size-m">Color</h2>
    <p class="t-body">When using this Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry's standard dummy text ever since the 1500s</p>

     <% icon_colors = [
      ["inherit (default)", "light"],
      ["-color-green", "light"],
      ["-color-red", "light"],
      ["-color-grey-dark", "light"],
      ["-color-white", "dark"],
    ] %>

    <div class="sg-grid">
      <% icon_colors.each do |icon_color, bg_color| %>
        <div class="sg-grid__item">
          <div class="sg-swatch">
            <div class="sg-swatch__header -size-large -background-<%= bg_color %>">
              <i class="e-icon -icon-envato <%= icon_color %> -size-jumbo"></i>
            </div>
            <div class="sg-swatch__body">
              <code><%= icon_color %></code>
            </div>
          </div>
        </div>
      <% end %>
    </div>

    <div class="sg-content__example">
      <h3 class="t-heading -size-xs">Preset Example</h3>
      <%= code_example do %>
        <span class="sg-code--inline example">
          <!-- A red icon -->
          <i class="e-icon -icon-envato -color-red -size-jumbo"></i>
        </span>
      <% end %>

      <h3 class="t-heading -size-xs h-spacing-above">Inheritance Example</h3>
      <%= code_example do %>
        <span class="sg-code--inline example">
          <!-- Inherit color -->
          <span style="color:orange;">
            <i class="e-icon -icon-envato -size-jumbo"></i>
          </span>
        </span>
      <% end %>
    </div>
  </section>


  <section class="sg-content__group">
    <h2 class="t-heading -size-m">Margin</h2>
    <p class="t-body">If you need left or right margin when placing an icon next to text.</p>

   <% icon_margins = [
      "default",
      "-margin-right",
      "-margin-left",
    ] %>

    <div class="sg-grid">
      <% icon_margins.each do |margin| %>
        <div class="sg-grid__item">
          <div class="sg-swatch">
            <div class="sg-swatch__header -size-large">
              |<i class="e-icon -icon-envato <%= margin %> -size-jumbo"></i>|
            </div>
            <div class="sg-swatch__body">
              <code><%= margin %></code>
            </div>
          </div>
        </div>
      <% end %>
    </div>

    <div class="sg-content__example">
      <h3 class="t-heading -size-xs">Example</h3>
      <%= code_example do %>
        <span class="sg-code--block example">
          <!-- Default margin -->
          |<i class="e-icon -icon-envato"></i>
          |<i class="e-icon -icon-envato -margin-right"></i>
          |<i class="e-icon -icon-envato -margin-left"></i>|
        </span>

        <span class="sg-code--block example">
          <!-- Medium icon margin -->
          |<i class="e-icon -icon-envato -size-medium"></i>
          |<i class="e-icon -icon-envato -size-medium -margin-right"></i>
          |<i class="e-icon -icon-envato -size-medium -margin-left"></i>|
        </span>

        <span class="sg-code--block example">
          <!-- Jumbo icon margin -->
          |<i class="e-icon -icon-envato -size-jumbo"></i>
          |<i class="e-icon -icon-envato -size-jumbo -margin-right"></i>
          |<i class="e-icon -icon-envato -size-jumbo -margin-left"></i>|
        </span>
      <% end %>
    </div>
  </section>


  <section class="sg-content__group">
    <h2 class="t-heading -size-m">Rotation</h2>
    <p class="t-body">You can rotate your icons by 90 degree increments or animate them spinning.</p>

   <% icon_rotations = [
      "default",
      "-rotate-90",
      "-rotate-180",
      "-rotate-270",
      "-spin-clockwise",
    ] %>

    <div class="sg-grid">
      <% icon_rotations.each do |rotate| %>
        <div class="sg-grid__item">
          <div class="sg-swatch">
            <div class="sg-swatch__header -size-large">
              |<i class="e-icon -icon-right-open <%= rotate %> -size-jumbo"></i>|
            </div>
            <div class="sg-swatch__body">
              <code><%= rotate %></code>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </section>


  <section class="sg-content__group">
    <h2 class="t-heading -size-m">Flipping</h2>
    <p class="t-body">You can rotate flip your icons horizontally and vertically. Most of the icons we have it would only ever make sense to flip either horizontally or vertically, not both.</p>

   <% icon_flips = [
      "default",
      "-flip-horizontal",
      "-flip-vertical",
    ] %>

    <div class="sg-grid">
      <% icon_flips.each do |flip| %>
        <div class="sg-grid__item">
          <div class="sg-swatch">
            <div class="sg-swatch__header -size-large">
              |<i class="e-icon -icon-envato <%= flip %> -size-jumbo"></i>|
            </div>
            <div class="sg-swatch__body">
              <code><%= flip %></code>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </section>


  <section class="sg-content__group">
    <h2 class="t-heading -size-m">Alt Content</h2>
    <p class="t-body">For search engines and screen-readers.</p>

    <div class="sg-content__example">
      <%= code_example do %>
        <span class="sg-code--block example">
          <!-- With alt text -->
          <i class="e-icon -icon-envato -size-jumbo"><span class="e-icon__alt">Envato Market</span></i>
        </span>

        <span class="sg-code--block example">
          <!-- Without alt text -->
          <i class="e-icon -icon-envato -size-jumbo">Envato Market</i>
        </span>
      <% end %>
    </div>
  </section>
</div>
